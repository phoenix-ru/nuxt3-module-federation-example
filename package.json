{
  "name": "nuxt3-module-federation-async-reproduction",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": {
    "node": "^14.0.0 || >=16.0.0",
    "npm": "^7.0.0 || ^8.0.0"
  }
}
